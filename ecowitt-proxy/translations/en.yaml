configuration:
  Webhook_ID:
    name: Webhook ID
    description: The unique ID for the webhook for the Ecowitt integration (shown on setup). Do not include /api/webhook/, only the 32-character webook ID generated by the Ecowitt integration.
  TCP_Port:
    name: TCP Port
    description: The port to listen on for Ecowitt weather station data.
